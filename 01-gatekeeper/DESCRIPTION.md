## Gatekeeper - Own The VM

You already have a VM in your browser. This module proves you can work like IR: over SSH, inside tmux, with huge logs and hostile editors.

Tools: `ssh`, `tmux`, `grep`, `cut`, `sort`, `uniq`, `vim`.

If stuck, study official `Linux Luminarium` after the workshop. No flags here are free - every one needs terminal fluency.

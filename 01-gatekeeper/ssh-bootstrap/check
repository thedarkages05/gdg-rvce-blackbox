#!/bin/sh
if [ -z "$SSH_CONNECTION" ]; then
  echo "Denied: no SSH_CONNECTION. Use ssh from WSL, not browser."
  exit 1
fi
if [ -z "$TMUX" ]; then
  echo "Denied: not in tmux. Run: tmux new -s ir"
  exit 1
fi
exec /challenge/print-flag
